#include <bits/stdc++.h>
using namespace std;

void explainMultimap() {
  // everything similar as map, only it can store multiple duplicate keys
  // in sorted manner
  // only mpp[] cannot be used here
}

void explainUnorderMap() {
  // same as set and unordered_set
  // it does not stores keys in sorted manner
}
