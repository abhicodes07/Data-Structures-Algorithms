
#include <bits/stdc++.h>
#include <unordered_set>
using namespace std;

void explainUnorderedSet() {
  // does not stores in any particular order but stores unique value
  // most functions same as set
  // better time complexity than set in most cases
  unordered_set<int> s;
}
